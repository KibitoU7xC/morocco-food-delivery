/**
 * API Client Configuration
 * Base HTTP client configuration (e.g., Axios instance or custom fetch wrapper)
 * 
 * Responsibilities:
 * - Base URL configuration (/api/v1)
 * - Authorization Header (Bearer Token injection from storage/cookies)
 * - Response & Error Interceptors
 */

export {};
