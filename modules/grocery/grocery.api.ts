/**
 * Grocery API Module
 * 
 * Endpoints:
 * - GET /api/v1/merchants/by-category/{groceries_category_id}
 *   Fetches supermarkets and grocery stores
 * 
 * - GET /api/v1/merchants/{merchant_id}/product-categories
 *   Fetches grocery aisles/departments (e.g., Produce, Dairy, Bakery, Beverages, Pantry)
 * 
 * - GET /api/v1/merchants/{merchant_id}/product-categories/{category_id}/products
 *   Fetches grocery items with stock quantity, pricing, and weights
 */

export {};
