/**
 * Pharmacy API Module
 * 
 * Endpoints:
 * - GET /api/v1/merchants/by-category/{pharmacy_category_id}
 *   Fetches licensed pharmacies and wellness stores
 * 
 * - GET /api/v1/merchants/{merchant_id}/product-categories
 *   Fetches pharmacy departments (OTC, First Aid, Vitamins, Baby Care, Personal Hygiene)
 * 
 * - GET /api/v1/merchants/{merchant_id}/product-categories/{category_id}/products
 *   Fetches pharmaceutical and healthcare products
 * 
 * - POST /api/v1/customer/tickets (or order attachments)
 *   Supports prescription image upload (multipart/form-data)
 */

export {};
