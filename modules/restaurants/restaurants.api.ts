/**
 * Restaurants API Module (Food Delivery)
 * 
 * Endpoints:
 * - GET /api/v1/restaurants
 *   Query params: search, category_id, page, limit
 * 
 * - GET /api/v1/restaurants/categories
 *   Response: List of food/restaurant cuisine categories (e.g., Fast Food, Traditional Moroccan, Pizza)
 * 
 * - GET /api/v1/restaurants/{id}
 *   Response: Restaurant profile, location coordinates, minimum order, average prep time, and menu products
 * 
 * - GET /api/v1/restaurants/{id}/reviews
 *   Response: Paginated customer reviews and average ratings (food & delivery)
 */

export {};
