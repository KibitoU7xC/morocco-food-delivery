/**
 * Promo Codes API Module
 * 
 * Endpoints:
 * - GET /api/v1/promocodes
 *   Optional query: ?restaurant_id={id}
 * 
 * - POST /api/v1/promocodes/apply
 *   Payload: { code: string }
 *   Response: Discount value, updated subtotal, and validated promo object
 */

export {};
