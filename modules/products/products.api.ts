/**
 * Products API Module
 * 
 * Endpoints:
 * - GET /api/v1/products
 *   Filters: search, category_id, min_price, max_price, is_veg, sort_by, sort_order, is_featured, is_trending, page, limit
 * 
 * - GET /api/v1/products/restaurant/{restaurant_id}
 *   Paginated and filtered menu items for a specific restaurant/vendor
 * 
 * - GET /api/v1/products/categories
 *   Hierarchical catalog categories
 * 
 * - GET /api/v1/products/{id}
 *   Detailed product data including variants, gallery images, and nutritional/dietary attributes
 */

export {};
